# WP0: Foundation (Fable)

Goal: everything the Codex packages depend on, committed on feature/stern-tab before WP1 and WP2 launch. Time box: 2 hours.

## Deliverables
1. Commit the plan docs (docs/plans/stern/**, scripts/stern-build/**, tests/fixtures/stern/**) as the first commit.
2. Migration: copy docs/plans/stern/schema/0029_stern.sql to db/migrations/0029_stern.sql unchanged. Run npm run migrate twice against the DB copy; both succeed.
3. lib/stern-types.ts (client-safe): every enum from the schema as const tuples plus display-label maps that match docs/plans/stern/design/STERN_DESIGN_BRIEF.md ("Need to reach out", "Reply received", "Interview invited", and so on); STERN_ROUTES const: /stern Overview, /stern/recruiting Club Recruiting, /stern/network Network, /stern/tasks Tasks, /stern/classes Classes, /stern/career Career, /stern/automation Automation. lib/stern-workspace.ts mirrors lib/business-workspace.ts (routes, legacy redirects: /school -> /stern, /career -> /stern/career).
4. Shell: app/stern/layout.tsx (getServerSession + allowedEmails + redirect("/signin"), wraps SternShell), components/stern/SternShell.tsx (rail 240px collapsible to 64px with localStorage persistence, items from STERN_ROUTES with lucide icons, header with page title, global search input placeholder, "Quick add" primary button that dispatches a window event "stern:quick-add", a sync status dot fed by the stern live channel, "Back to dashboard" link, Escape returns to /), components/stern/Page.tsx primitives: SternPage, SternSection, StatTile, StatusChip (takes an enum value and renders the display label with the right status color), StrengthDots, SourceBadge (Manual, Auto (email), Auto (calendar), Auto (iMessage), Suggested), EmptyState, SkeletonRows.
5. Theme: app/globals.css gets a .stern-mode scope using the variables in docs/plans/stern/design/stern-design-tokens.css, plus the same shared-class remaps .business-mode uses so ConnectionsPanel, AccountsPanel, and CareerWorkspace render correctly in light mode. Fonts: Inter and JetBrains Mono are already loaded for the app; reuse them.
6. Pages: app/stern/page.tsx and one page.tsx per route above, each rendering SternPage with an honest EmptyState ("Built in WP1" style copy is not allowed; say what data is missing, for example "No clubs tracked yet"). app/(dash)/school/page.tsx and lib/school.ts and app/api/school/route.ts are deleted after checking usages (components/home/Home.tsx may reference the term countdown; remove that tile). app/(dash)/career/page.tsx becomes a redirect to /stern/career. components/shell/nav.tsx: append { key: "stern", label: "Stern", href: "/stern", Icon: GraduationCap }; remove the School and Career entries; keep the order comment honest.
7. API and live: app/api/stern/route.ts GET returns lib/stern/snapshot.ts output (per-area counts and the pieces WP1 to WP6 will fill; empty arrays are fine now); POST rejects unknown actions with 400. server/scheduler.ts: tickStern every 15 s (guarded, boot burst) broadcasting channel "stern". lib/stern/errors.ts with SternError(status, message).
8. Vault write helper: lib/stern/vault-write.ts exporting upsertNote(relPath, frontmatter, body) that writes only under <vault>/Stern/, creates folders, never deletes, no-ops when the vault root is missing or STERN_VAULT_WRITE is "0", and is a no-op in tests by default. Unit test with a temp vault.
9. Audit helper: lib/stern/audit.ts exporting logChange({entityType, entityId, action, field, before, after, source, confidence, evidence, batchId}) and undoBatch(batchId) that restores before values for updates and deletes rows created in the batch (entity tables enumerated explicitly). Unit tests.
10. CLI writer: scripts/stern-cli.ts (trusted local writer, no network, IMMEDIATE transactions, JSON in and out) with commands add-person, add-task, add-touchpoint, set-person-status, list-people (search), each writing audit rows with source "imessage" or "agent" per a --source flag. Registered as npm run stern-cli. Usage doc at the top of the file.
11. Tests: tests/stern-workspace.test.ts (every STERN_ROUTES entry has a page.tsx; legacy redirects exist), tests/stern-foundation.test.ts (migration idempotent on a temp DB, audit log + undo, vault helper, CLI add-person round trip).
12. Register the build session: npm run agent-event -- --agent rathworkspace-platform-developer --run stern-build-<ts> --kind started --status running --summary "Stern tab weekend build started" --trigger-type manual --trigger-source "Claude Code orchestrator". Update PROGRESS.md. Gate PASS. Commit. Email "WP0 foundation merged".

## Acceptance checklist
- [ ] db/migrations/0029_stern.sql identical to the schema doc; migrate twice passes on a DB copy.
- [ ] /stern and all six sub-routes render inside SternShell with the light theme and honest empty states; /school and /career redirect.
- [ ] nav shows Stern, no School, no Career; command palette follows.
- [ ] GET /api/stern returns a snapshot; the "stern" channel broadcasts every 15 s.
- [ ] vault-write, audit + undo, and stern-cli have passing tests.
- [ ] gate.sh PASS on the integration worktree; git status clean; PROGRESS.md updated; email sent.
