# deps
node_modules/
.pnp
.pnp.js

# next
.next/
out/
next-env.d.ts

# data (sqlite lives on the box, never in git)
data/*.db
data/**/*.db
data/*.db-*
data/*.db.backup*
data/*.backup*
data/*.sqlite*

# secrets — the real store is ~/.config/rathworkspace/secrets.env, never here
.env
.env.local
.env*.local
*.secret

# logs / os
npm-install.log
*.log
logs/
.DS_Store

# python cache from agent script smoke checks
__pycache__/
*.py[cod]

# screenshots / build artifacts
shots/
build.log
server-test.log

# TS incremental build cache
tsconfig.tsbuildinfo
