# deps
node_modules/
.pnp
.pnp.js

# next
.next/
out/
next-env.d.ts

# data (sqlite and operator-local config live on the box, never in git)
data/
!data/.gitkeep

# secrets — the real store is outside the repo, never here
.env
.env.local
.env*.local
*.secret
secrets.env
*.secrets.env
*.pem
*.key
*.p12

# logs / os
npm-install.log
*.log
logs/
.DS_Store

# python cache from agent script smoke checks
__pycache__/
*.py[cod]

# screenshots / build artifacts
shots/
build.log
server-test.log

# TS incremental build cache
tsconfig.tsbuildinfo

# local databases anywhere
*.db
*.sqlite
*.sqlite3

# session / working artifacts
SESSION_PROGRESS.md
.pytest_cache/

# Isolated Stern Network tests and browser artifacts
.stern-network-test-*/
.stern-network-e2e/
